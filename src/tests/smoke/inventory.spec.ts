import { expect, test } from "../../fixtures";

test("verify all ui elements are displayed", async ({ pageInventory }) => {});

test("add one item to cart", async ({ pageInventory }) => {});

test("add and remove item from cart", async ({ pageInventory }) => {});
